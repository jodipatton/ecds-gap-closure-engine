# Optional. Without this key the agent chat falls back to a tool-only mode that
# echoes structured tool-call results without LLM-generated prose.
OPENAI_API_KEY=

# Optional. The repository layer is JSON-file backed by default.
# Set to "firestore" later to swap in a Firestore implementation.
ECDS_DATA_BACKEND=json
